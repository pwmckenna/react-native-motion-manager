
include ':react-native-connected-common'
// First check the parent directory
def parentLocation = new File(rootProject.projectDir, '../react-native-connected-common');
if (parentLocation.isDirectory() && parentLocation.exists()) {
    project(':react-native-connected-common').projectDir = parentLocation;
} else {
    project(':react-native-connected-common').projectDir = new File(rootProject.projectDir, '../node_modules/react-native-connected-common/android')
}
